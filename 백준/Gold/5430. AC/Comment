처음에 배열로 만들려다가 너무 비효율적일 것 같아 Deque로 바꿈
처음에 구현했을 때 for문 안에 for(int i = 0; i < q.size(); i++)같은 방식으로 했는데 q.size가 for문 내부에서 바뀌어서 for문이 제대로 안돌아가서 고침
그 이후 구현해서 잘 실행되었으나 틀렸습니다가 떠서 봤더니 큐가 비어있어도 순서를 뒤집는건 error가 아니라 고쳐서 풀이 완료
